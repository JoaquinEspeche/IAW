# Port configuration
PORT=3000

# MongoDB Atlas / Local Connection String
# Replace the line below with your MongoDB Atlas URI:
# e.g., MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/search-service?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/search-service

# Environment
NODE_ENV=development
